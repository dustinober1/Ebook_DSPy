{
  "name": "dspy-ebook-static",
  "version": "1.0.0",
  "description": "Static version of DSPy: A Practical Guide",
  "scripts": {
    "build": "node build.js",
    "serve": "python -m http.server 8000 --directory static",
    "deploy": "npm run build && gh-pages --dist static --add"
  },
  "dependencies": {
    "markdown-it": "^13.0.2",
    "markdown-it-anchor": "^8.6.7",
    "markdown-it-table-of-contents": "^0.6.0",
    "markdown-it-highlightjs": "^3.6.0",
    "fs-extra": "^11.2.0",
    "path": "^0.12.7",
    "front-matter": "^4.0.2"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}